type Header = {
  menu: {
    title: string;
    label: string;
    open: {
      label: string;
    };
    close: {
      label: string;
    };
  };
  locale: {
    label: string;
  };
};

export type { Header };
