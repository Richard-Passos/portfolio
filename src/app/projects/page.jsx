import { ProjectsView } from '@/views';

const ProjectsPage = () => {
  return <ProjectsView />;
};

const metadata = {
  title: 'Projects',
  description: 'Richard passos projects.',
 }

export default ProjectsPage;
export {metadata}
