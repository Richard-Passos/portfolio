import CleanLayouts from './Clean';
import PrimaryLayouts from './Primary';
import SecondaryLayouts from './Secondary';

const Layoutss = {
  Clean: CleanLayouts,
  Primary: PrimaryLayouts,
  Secondary: SecondaryLayouts
};

export default Layoutss;
export { CleanLayouts, PrimaryLayouts, SecondaryLayouts };
