import { ScrollAnimateRootConfigOptions } from '../Root';

const lineLeft: ScrollAnimateRootConfigOptions = {
  scrollConfig: {
    offset: ['0 1', '0 .5']
  },
  prop: 'left',
  propPoints: ['100%', '0%']
};

export { lineLeft };
