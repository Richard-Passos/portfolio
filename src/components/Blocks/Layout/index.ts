import CleanLayoutBlock from './Clean';
import PrimaryLayoutBlock from './Primary';
import SecondaryLayoutBlock from './Secondary';

const LayoutBlocks = {
  Clean: CleanLayoutBlock,
  Primary: PrimaryLayoutBlock,
  Secondary: SecondaryLayoutBlock
};

export default LayoutBlocks;
export { CleanLayoutBlock, PrimaryLayoutBlock, SecondaryLayoutBlock };
