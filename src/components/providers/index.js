import GlobalStateProvider from './GlobalState';
import ThemeProvider from './Theme';

const Providers = {
  GlobalState: GlobalStateProvider,
  Theme: ThemeProvider,
};

export default Providers;
export { GlobalStateProvider, ThemeProvider };
