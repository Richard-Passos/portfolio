const defaultPages = {
  home: 'home',
  projects: 'projects',
  singleProject: 'single-project',
  revalidate: 'revalidate',
  error: 'error',
  notFound: 'not-found'
};

export default defaultPages;
