import personalInfo from '../personalInfo';

const introText = `Hey there! I'm ${
  personalInfo.name.first
} an awesome ${personalInfo.job.toLowerCase()} — who cares building solid and scalable products with a great user experience.`;

export default introText;
